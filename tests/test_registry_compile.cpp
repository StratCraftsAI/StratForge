// AUTO-GENERATED by tools/generate_compile_tests.py
// DO NOT EDIT MANUALLY - regenerate with:
//   python3 tools/generate_compile_tests.py

#include <catch2/catch_test_macros.hpp>
#include <stratforge/data/csv_data.hpp>
#include <stratforge/indicators/sma.hpp>  // for dual_line fixtures

#include <stratforge/indicators/accum.hpp>
#include <stratforge/indicators/adaptive.hpp>
#include <stratforge/indicators/aroon.hpp>
#include <stratforge/indicators/atr.hpp>
#include <stratforge/indicators/average.hpp>
#include <stratforge/indicators/awesomeoscillator.hpp>
#include <stratforge/indicators/bollinger.hpp>
#include <stratforge/indicators/bollingerpct.hpp>
#include <stratforge/indicators/cci.hpp>
#include <stratforge/indicators/crossover.hpp>
#include <stratforge/indicators/dema.hpp>
#include <stratforge/indicators/directionalmovement.hpp>
#include <stratforge/indicators/dma.hpp>
#include <stratforge/indicators/downday.hpp>
#include <stratforge/indicators/downmove.hpp>
#include <stratforge/indicators/dpo.hpp>
#include <stratforge/indicators/dv2.hpp>
#include <stratforge/indicators/ema.hpp>
#include <stratforge/indicators/envelope.hpp>
#include <stratforge/indicators/exponentialsmoothing.hpp>
#include <stratforge/indicators/findindex.hpp>
#include <stratforge/indicators/hadelta.hpp>
#include <stratforge/indicators/heikinashi.hpp>
#include <stratforge/indicators/highest.hpp>
#include <stratforge/indicators/hma.hpp>
#include <stratforge/indicators/hurst.hpp>
#include <stratforge/indicators/ichimoku.hpp>
#include <stratforge/indicators/kama.hpp>
#include <stratforge/indicators/kst.hpp>
#include <stratforge/indicators/laguerre.hpp>
#include <stratforge/indicators/logicaln.hpp>
#include <stratforge/indicators/lowest.hpp>
#include <stratforge/indicators/macd.hpp>
#include <stratforge/indicators/meandeviation.hpp>
#include <stratforge/indicators/momentum.hpp>
#include <stratforge/indicators/obv.hpp>
#include <stratforge/indicators/ols.hpp>
#include <stratforge/indicators/oscillator.hpp>
#include <stratforge/indicators/oscillator_extra.hpp>
#include <stratforge/indicators/pattern.hpp>
#include <stratforge/indicators/pctrank.hpp>
#include <stratforge/indicators/percentchange.hpp>
#include <stratforge/indicators/pivotpoint.hpp>
#include <stratforge/indicators/prettygoodoscillator.hpp>
#include <stratforge/indicators/psar.hpp>
#include <stratforge/indicators/rsi.hpp>
#include <stratforge/indicators/sma.hpp>
#include <stratforge/indicators/smma.hpp>
#include <stratforge/indicators/statistics.hpp>
#include <stratforge/indicators/stddev.hpp>
#include <stratforge/indicators/stochastic.hpp>
#include <stratforge/indicators/sumn.hpp>
#include <stratforge/indicators/tema.hpp>
#include <stratforge/indicators/trend.hpp>
#include <stratforge/indicators/trix.hpp>
#include <stratforge/indicators/truehigh.hpp>
#include <stratforge/indicators/truelow.hpp>
#include <stratforge/indicators/truerange.hpp>
#include <stratforge/indicators/tsi.hpp>
#include <stratforge/indicators/ultimateoscillator.hpp>
#include <stratforge/indicators/upday.hpp>
#include <stratforge/indicators/upmove.hpp>
#include <stratforge/indicators/volatility.hpp>
#include <stratforge/indicators/volume.hpp>
#include <stratforge/indicators/vortex.hpp>
#include <stratforge/indicators/weightedaverage.hpp>
#include <stratforge/indicators/williams.hpp>
#include <stratforge/indicators/wma.hpp>
#include <stratforge/indicators/zerolag.hpp>
#include <stratforge/indicators/zlema.hpp>

#include <string>

TEST_CASE("Registry compile: ADX", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ADX adx_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    adx_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        adx_.next();
    }

    [[maybe_unused]] const auto& ref_line = adx_.line();
    [[maybe_unused]] auto op0 = adx_[0];
}

TEST_CASE("Registry compile: ADXR", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ADXR adxr_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    adxr_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        adxr_.next();
    }

    [[maybe_unused]] const auto& ref_line = adxr_.line();
    [[maybe_unused]] auto op0 = adxr_[0];
}

TEST_CASE("Registry compile: ATR", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ATR atr_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    atr_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        atr_.next();
    }

    [[maybe_unused]] const auto& ref_line = atr_.line();
    [[maybe_unused]] auto op0 = atr_[0];
}

TEST_CASE("Registry compile: AccumulationDistribution", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AccumulationDistribution accumulationdistribution_(data.high(), data.low(), data.close(), data.volume());

    // Process enough bars (preload sets index to 0)
    accumulationdistribution_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        accumulationdistribution_.next();
    }

    [[maybe_unused]] const auto& ref_line = accumulationdistribution_.line();
    [[maybe_unused]] auto op0 = accumulationdistribution_[0];
}

TEST_CASE("Registry compile: AllN", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AllN alln_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    alln_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        alln_.next();
    }

    [[maybe_unused]] const auto& ref_line = alln_.line();
    [[maybe_unused]] auto op0 = alln_[0];
}

TEST_CASE("Registry compile: AnyN", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AnyN anyn_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    anyn_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        anyn_.next();
    }

    [[maybe_unused]] const auto& ref_line = anyn_.line();
    [[maybe_unused]] auto op0 = anyn_[0];
}

TEST_CASE("Registry compile: Aroon", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Aroon aroon_(data.high(), data.low(), 25);

    // Process enough bars (preload sets index to 0)
    aroon_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        aroon_.next();
    }

    [[maybe_unused]] const auto& ref_up = aroon_.up();
    [[maybe_unused]] const auto& ref_down = aroon_.down();
    [[maybe_unused]] auto op0 = aroon_[0];
}

TEST_CASE("Registry compile: AroonDown", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AroonDown aroondown_(data.low(), 14);

    // Process enough bars (preload sets index to 0)
    aroondown_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        aroondown_.next();
    }

    [[maybe_unused]] const auto& ref_line = aroondown_.line();
    [[maybe_unused]] auto op0 = aroondown_[0];
}

TEST_CASE("Registry compile: AroonOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AroonOscillator aroonoscillator_(data.high(), data.low(), 14);

    // Process enough bars (preload sets index to 0)
    aroonoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        aroonoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_up = aroonoscillator_.up();
    [[maybe_unused]] const auto& ref_down = aroonoscillator_.down();
    [[maybe_unused]] auto op0 = aroonoscillator_[0];
}

TEST_CASE("Registry compile: AroonUp", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AroonUp aroonup_(data.high(), 14);

    // Process enough bars (preload sets index to 0)
    aroonup_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        aroonup_.next();
    }

    [[maybe_unused]] const auto& ref_line = aroonup_.line();
    [[maybe_unused]] auto op0 = aroonup_[0];
}

TEST_CASE("Registry compile: AroonUpDown", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AroonUpDown aroonupdown_(data.high(), data.low(), 14);

    // Process enough bars (preload sets index to 0)
    aroonupdown_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        aroonupdown_.next();
    }

    [[maybe_unused]] const auto& ref_up = aroonupdown_.up();
    [[maybe_unused]] const auto& ref_down = aroonupdown_.down();
    [[maybe_unused]] const auto& ref_aroonup = aroonupdown_.aroonup();
    [[maybe_unused]] const auto& ref_aroondown = aroonupdown_.aroondown();
    [[maybe_unused]] auto op0 = aroonupdown_[0];
}

TEST_CASE("Registry compile: AroonUpDownOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AroonUpDownOscillator aroonupdownoscillator_(data.high(), data.low(), 14);

    // Process enough bars (preload sets index to 0)
    aroonupdownoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        aroonupdownoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_up = aroonupdownoscillator_.up();
    [[maybe_unused]] const auto& ref_down = aroonupdownoscillator_.down();
    [[maybe_unused]] auto op0 = aroonupdownoscillator_[0];
}

TEST_CASE("Registry compile: Average", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Average average_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    average_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        average_.next();
    }

    [[maybe_unused]] const auto& ref_line = average_.line();
    [[maybe_unused]] auto op0 = average_[0];
}

TEST_CASE("Registry compile: AwesomeOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::AwesomeOscillator awesomeoscillator_(data.high(), data.low(), 5, 34);

    // Process enough bars (preload sets index to 0)
    awesomeoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        awesomeoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = awesomeoscillator_.line();
    [[maybe_unused]] auto op0 = awesomeoscillator_[0];
}

TEST_CASE("Registry compile: BollingerBands", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::BollingerBands bollingerbands_(data.close(), 20, 2.0);

    // Process enough bars (preload sets index to 0)
    bollingerbands_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        bollingerbands_.next();
    }

    [[maybe_unused]] const auto& ref_mid = bollingerbands_.mid();
    [[maybe_unused]] const auto& ref_top = bollingerbands_.top();
    [[maybe_unused]] const auto& ref_bottom = bollingerbands_.bottom();
    [[maybe_unused]] auto op0 = bollingerbands_[0];
}

TEST_CASE("Registry compile: BollingerBandsPct", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::BollingerBandsPct bollingerbandspct_(data.close(), 20, 2.0);

    // Process enough bars (preload sets index to 0)
    bollingerbandspct_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        bollingerbandspct_.next();
    }

    [[maybe_unused]] const auto& ref_mid = bollingerbandspct_.mid();
    [[maybe_unused]] const auto& ref_top = bollingerbandspct_.top();
    [[maybe_unused]] const auto& ref_bottom = bollingerbandspct_.bottom();
    [[maybe_unused]] const auto& ref_pctb = bollingerbandspct_.pctb();
    [[maybe_unused]] auto op0 = bollingerbandspct_[0];
}

TEST_CASE("Registry compile: CCI", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::CCI cci_(data.high(), data.low(), data.close(), 20, 0.015);

    // Process enough bars (preload sets index to 0)
    cci_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        cci_.next();
    }

    [[maybe_unused]] const auto& ref_line = cci_.line();
    [[maybe_unused]] auto op0 = cci_[0];
}

TEST_CASE("Registry compile: ChaikinMoneyFlow", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ChaikinMoneyFlow chaikinmoneyflow_(data.high(), data.low(), data.close(), data.volume(), 20);

    // Process enough bars (preload sets index to 0)
    chaikinmoneyflow_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        chaikinmoneyflow_.next();
    }

    [[maybe_unused]] const auto& ref_line = chaikinmoneyflow_.line();
    [[maybe_unused]] auto op0 = chaikinmoneyflow_[0];
}

TEST_CASE("Registry compile: ChaikinVolatility", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ChaikinVolatility chaikinvolatility_(data.high(), data.low(), 10, 10);

    // Process enough bars (preload sets index to 0)
    chaikinvolatility_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        chaikinvolatility_.next();
    }

    [[maybe_unused]] const auto& ref_line = chaikinvolatility_.line();
    [[maybe_unused]] auto op0 = chaikinvolatility_[0];
}

TEST_CASE("Registry compile: ChandeMomentumOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ChandeMomentumOscillator chandemomentumoscillator_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    chandemomentumoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        chandemomentumoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = chandemomentumoscillator_.line();
    [[maybe_unused]] auto op0 = chandemomentumoscillator_[0];
}

TEST_CASE("Registry compile: ChandelierExit", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ChandelierExit chandelierexit_(data.high(), data.low(), data.close(), 22, 3.0);

    // Process enough bars (preload sets index to 0)
    chandelierexit_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        chandelierexit_.next();
    }

    [[maybe_unused]] const auto& ref_long_exit = chandelierexit_.long_exit();
    [[maybe_unused]] const auto& ref_short_exit = chandelierexit_.short_exit();
    [[maybe_unused]] auto op0 = chandelierexit_[0];
}

TEST_CASE("Registry compile: CoppockCurve", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::CoppockCurve coppockcurve_(data.close(), 11, 14, 10);

    // Process enough bars (preload sets index to 0)
    coppockcurve_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        coppockcurve_.next();
    }

    [[maybe_unused]] const auto& ref_line = coppockcurve_.line();
    [[maybe_unused]] auto op0 = coppockcurve_[0];
}

TEST_CASE("Registry compile: Correlation", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::Correlation correlation_(sma_a_.line(), sma_b_.line(), 20);

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    correlation_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        correlation_.next();
    }

    [[maybe_unused]] const auto& ref_line = correlation_.line();
    [[maybe_unused]] auto op0 = correlation_[0];
}

TEST_CASE("Registry compile: CrossDown", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::CrossDown crossdown_(sma_a_.line(), sma_b_.line());

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    crossdown_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        crossdown_.next();
    }

    [[maybe_unused]] const auto& ref_line = crossdown_.line();
    [[maybe_unused]] auto op0 = crossdown_[0];
}

TEST_CASE("Registry compile: CrossOver", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::CrossOver crossover_(sma_a_.line(), sma_b_.line());

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    crossover_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        crossover_.next();
    }

    [[maybe_unused]] const auto& ref_line = crossover_.line();
    [[maybe_unused]] auto op0 = crossover_[0];
}

TEST_CASE("Registry compile: CrossUp", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::CrossUp crossup_(sma_a_.line(), sma_b_.line());

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    crossup_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        crossup_.next();
    }

    [[maybe_unused]] const auto& ref_line = crossup_.line();
    [[maybe_unused]] auto op0 = crossup_[0];
}

TEST_CASE("Registry compile: CumSum", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::CumSum cumsum_(data.close(), 0.0);

    // Process enough bars (preload sets index to 0)
    cumsum_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        cumsum_.next();
    }

    [[maybe_unused]] const auto& ref_line = cumsum_.line();
    [[maybe_unused]] auto op0 = cumsum_[0];
}

TEST_CASE("Registry compile: DEMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DEMA dema_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    dema_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        dema_.next();
    }

    [[maybe_unused]] const auto& ref_line = dema_.line();
    [[maybe_unused]] auto op0 = dema_[0];
}

TEST_CASE("Registry compile: DEMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DEMAEnvelope demaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    demaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        demaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = demaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = demaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = demaenvelope_.bottom();
    [[maybe_unused]] auto op0 = demaenvelope_[0];
}

TEST_CASE("Registry compile: DEMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DEMAOscillator demaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    demaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        demaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = demaoscillator_.line();
    [[maybe_unused]] auto op0 = demaoscillator_[0];
}

TEST_CASE("Registry compile: DMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DMA dma_(data.close(), 20, 50, 7);

    // Process enough bars (preload sets index to 0)
    dma_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        dma_.next();
    }

    [[maybe_unused]] const auto& ref_line = dma_.line();
    [[maybe_unused]] auto op0 = dma_[0];
}

TEST_CASE("Registry compile: DMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DMAEnvelope dmaenvelope_(data.close(), 20, 50, 7, 2.5);

    // Process enough bars (preload sets index to 0)
    dmaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        dmaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = dmaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = dmaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = dmaenvelope_.bottom();
    [[maybe_unused]] auto op0 = dmaenvelope_[0];
}

TEST_CASE("Registry compile: DMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DMAOscillator dmaoscillator_(data.close(), 20, 50, 7);

    // Process enough bars (preload sets index to 0)
    dmaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        dmaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = dmaoscillator_.line();
    [[maybe_unused]] auto op0 = dmaoscillator_[0];
}

TEST_CASE("Registry compile: DV2", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DV2 dv2_(data.high(), data.low(), data.close(), 252, 2);

    // Process enough bars (preload sets index to 0)
    dv2_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        dv2_.next();
    }

    [[maybe_unused]] const auto& ref_line = dv2_.line();
    [[maybe_unused]] auto op0 = dv2_[0];
}

TEST_CASE("Registry compile: DemarkPivotPoint", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DemarkPivotPoint demarkpivotpoint_(data.open(), data.high(), data.low(), data.close());

    // Process enough bars (preload sets index to 0)
    demarkpivotpoint_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        demarkpivotpoint_.next();
    }

    [[maybe_unused]] const auto& ref_p = demarkpivotpoint_.p();
    [[maybe_unused]] const auto& ref_s1 = demarkpivotpoint_.s1();
    [[maybe_unused]] const auto& ref_r1 = demarkpivotpoint_.r1();
    [[maybe_unused]] auto op0 = demarkpivotpoint_[0];
}

TEST_CASE("Registry compile: DetrendedPriceOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DetrendedPriceOscillator detrendedpriceoscillator_(data.close(), 20);

    // Process enough bars (preload sets index to 0)
    detrendedpriceoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        detrendedpriceoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = detrendedpriceoscillator_.line();
    [[maybe_unused]] auto op0 = detrendedpriceoscillator_[0];
}

TEST_CASE("Registry compile: DirectionalIndicator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DirectionalIndicator directionalindicator_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    directionalindicator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        directionalindicator_.next();
    }

    [[maybe_unused]] const auto& ref_plus_di = directionalindicator_.plus_di();
    [[maybe_unused]] const auto& ref_minus_di = directionalindicator_.minus_di();
    [[maybe_unused]] auto op0 = directionalindicator_[0];
}

TEST_CASE("Registry compile: DirectionalMovement", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DirectionalMovement directionalmovement_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    directionalmovement_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        directionalmovement_.next();
    }

    [[maybe_unused]] const auto& ref_plus_di = directionalmovement_.plus_di();
    [[maybe_unused]] const auto& ref_minus_di = directionalmovement_.minus_di();
    [[maybe_unused]] const auto& ref_adx = directionalmovement_.adx();
    [[maybe_unused]] const auto& ref_adxr = directionalmovement_.adxr();
    [[maybe_unused]] auto op0 = directionalmovement_[0];
}

TEST_CASE("Registry compile: DonchianChannels", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DonchianChannels donchianchannels_(data.high(), data.low(), 20);

    // Process enough bars (preload sets index to 0)
    donchianchannels_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        donchianchannels_.next();
    }

    [[maybe_unused]] const auto& ref_mid = donchianchannels_.mid();
    [[maybe_unused]] const auto& ref_top = donchianchannels_.top();
    [[maybe_unused]] const auto& ref_bottom = donchianchannels_.bottom();
    [[maybe_unused]] auto op0 = donchianchannels_[0];
}

TEST_CASE("Registry compile: DownDay", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DownDay downday_(data.close(), 1);

    // Process enough bars (preload sets index to 0)
    downday_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        downday_.next();
    }

    [[maybe_unused]] const auto& ref_line = downday_.line();
    [[maybe_unused]] auto op0 = downday_[0];
}

TEST_CASE("Registry compile: DownDayBool", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DownDayBool downdaybool_(data.close(), 1);

    // Process enough bars (preload sets index to 0)
    downdaybool_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        downdaybool_.next();
    }

    [[maybe_unused]] const auto& ref_line = downdaybool_.line();
    [[maybe_unused]] auto op0 = downdaybool_[0];
}

TEST_CASE("Registry compile: DownMove", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::DownMove downmove_(data.low());

    // Process enough bars (preload sets index to 0)
    downmove_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        downmove_.next();
    }

    [[maybe_unused]] const auto& ref_line = downmove_.line();
    [[maybe_unused]] auto op0 = downmove_[0];
}

TEST_CASE("Registry compile: EMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::EMA ema_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    ema_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        ema_.next();
    }

    [[maybe_unused]] const auto& ref_line = ema_.line();
    [[maybe_unused]] auto op0 = ema_[0];
}

TEST_CASE("Registry compile: EMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::EMAEnvelope emaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    emaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        emaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = emaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = emaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = emaenvelope_.bottom();
    [[maybe_unused]] auto op0 = emaenvelope_[0];
}

TEST_CASE("Registry compile: EMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::EMAOscillator emaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    emaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        emaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = emaoscillator_.line();
    [[maybe_unused]] auto op0 = emaoscillator_[0];
}

TEST_CASE("Registry compile: EaseOfMovement", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::EaseOfMovement easeofmovement_(data.high(), data.low(), data.volume(), 14, 10000.0);

    // Process enough bars (preload sets index to 0)
    easeofmovement_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        easeofmovement_.next();
    }

    [[maybe_unused]] const auto& ref_line = easeofmovement_.line();
    [[maybe_unused]] auto op0 = easeofmovement_[0];
}

TEST_CASE("Registry compile: Envelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Envelope envelope_(data.close(), 2.5);

    // Process enough bars (preload sets index to 0)
    envelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        envelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = envelope_.src();
    [[maybe_unused]] const auto& ref_top = envelope_.top();
    [[maybe_unused]] const auto& ref_bottom = envelope_.bottom();
    [[maybe_unused]] auto op0 = envelope_[0];
}

TEST_CASE("Registry compile: ExponentialSmoothing", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ExponentialSmoothing exponentialsmoothing_(data.close(), 1, 0.5);

    // Process enough bars (preload sets index to 0)
    exponentialsmoothing_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        exponentialsmoothing_.next();
    }

    [[maybe_unused]] const auto& ref_line = exponentialsmoothing_.line();
    [[maybe_unused]] auto op0 = exponentialsmoothing_[0];
}

TEST_CASE("Registry compile: ExponentialSmoothingDynamic", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::ExponentialSmoothingDynamic exponentialsmoothingdynamic_(sma_a_.line(), sma_b_.line(), 1);

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    exponentialsmoothingdynamic_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        exponentialsmoothingdynamic_.next();
    }

    [[maybe_unused]] const auto& ref_line = exponentialsmoothingdynamic_.line();
    [[maybe_unused]] auto op0 = exponentialsmoothingdynamic_[0];
}

TEST_CASE("Registry compile: FRAMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::FRAMA frama_(data.close(), 16, 4, 300);

    // Process enough bars (preload sets index to 0)
    frama_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        frama_.next();
    }

    [[maybe_unused]] const auto& ref_line = frama_.line();
    [[maybe_unused]] auto op0 = frama_[0];
}

TEST_CASE("Registry compile: FibonacciPivotPoint", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::FibonacciPivotPoint fibonaccipivotpoint_(data.open(), data.high(), data.low(), data.close(), false, false, 0.382, 0.618, 1.0);

    // Process enough bars (preload sets index to 0)
    fibonaccipivotpoint_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        fibonaccipivotpoint_.next();
    }

    [[maybe_unused]] const auto& ref_p = fibonaccipivotpoint_.p();
    [[maybe_unused]] const auto& ref_s1 = fibonaccipivotpoint_.s1();
    [[maybe_unused]] const auto& ref_s2 = fibonaccipivotpoint_.s2();
    [[maybe_unused]] const auto& ref_s3 = fibonaccipivotpoint_.s3();
    [[maybe_unused]] const auto& ref_r1 = fibonaccipivotpoint_.r1();
    [[maybe_unused]] const auto& ref_r2 = fibonaccipivotpoint_.r2();
    [[maybe_unused]] const auto& ref_r3 = fibonaccipivotpoint_.r3();
    [[maybe_unused]] auto op0 = fibonaccipivotpoint_[0];
}

TEST_CASE("Registry compile: FindFirstIndexHighest", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::FindFirstIndexHighest findfirstindexhighest_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    findfirstindexhighest_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        findfirstindexhighest_.next();
    }

    [[maybe_unused]] const auto& ref_line = findfirstindexhighest_.line();
    [[maybe_unused]] auto op0 = findfirstindexhighest_[0];
}

TEST_CASE("Registry compile: FindFirstIndexLowest", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::FindFirstIndexLowest findfirstindexlowest_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    findfirstindexlowest_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        findfirstindexlowest_.next();
    }

    [[maybe_unused]] const auto& ref_line = findfirstindexlowest_.line();
    [[maybe_unused]] auto op0 = findfirstindexlowest_[0];
}

TEST_CASE("Registry compile: FindLastIndexHighest", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::FindLastIndexHighest findlastindexhighest_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    findlastindexhighest_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        findlastindexhighest_.next();
    }

    [[maybe_unused]] const auto& ref_line = findlastindexhighest_.line();
    [[maybe_unused]] auto op0 = findlastindexhighest_[0];
}

TEST_CASE("Registry compile: FindLastIndexLowest", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::FindLastIndexLowest findlastindexlowest_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    findlastindexlowest_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        findlastindexlowest_.next();
    }

    [[maybe_unused]] const auto& ref_line = findlastindexlowest_.line();
    [[maybe_unused]] auto op0 = findlastindexlowest_[0];
}

TEST_CASE("Registry compile: FisherTransform", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::FisherTransform fishertransform_(data.high(), data.low(), 10);

    // Process enough bars (preload sets index to 0)
    fishertransform_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        fishertransform_.next();
    }

    [[maybe_unused]] const auto& ref_fisher = fishertransform_.fisher();
    [[maybe_unused]] const auto& ref_trigger = fishertransform_.trigger();
    [[maybe_unused]] auto op0 = fishertransform_[0];
}

TEST_CASE("Registry compile: Fractal", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Fractal fractal_(data.high(), data.low());

    // Process enough bars (preload sets index to 0)
    fractal_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        fractal_.next();
    }

    [[maybe_unused]] const auto& ref_up_fractal = fractal_.up_fractal();
    [[maybe_unused]] const auto& ref_down_fractal = fractal_.down_fractal();
    [[maybe_unused]] auto op0 = fractal_[0];
}

TEST_CASE("Registry compile: HMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::HMA hma_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    hma_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        hma_.next();
    }

    [[maybe_unused]] const auto& ref_line = hma_.line();
    [[maybe_unused]] auto op0 = hma_[0];
}

TEST_CASE("Registry compile: HMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::HMAEnvelope hmaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    hmaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        hmaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = hmaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = hmaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = hmaenvelope_.bottom();
    [[maybe_unused]] auto op0 = hmaenvelope_[0];
}

TEST_CASE("Registry compile: HMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::HMAOscillator hmaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    hmaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        hmaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = hmaoscillator_.line();
    [[maybe_unused]] auto op0 = hmaoscillator_[0];
}

TEST_CASE("Registry compile: HeikinAshi", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::HeikinAshi heikinashi_(data.open(), data.high(), data.low(), data.close());

    // Process enough bars (preload sets index to 0)
    heikinashi_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        heikinashi_.next();
    }

    [[maybe_unused]] const auto& ref_ha_open = heikinashi_.ha_open();
    [[maybe_unused]] const auto& ref_ha_high = heikinashi_.ha_high();
    [[maybe_unused]] const auto& ref_ha_low = heikinashi_.ha_low();
    [[maybe_unused]] const auto& ref_ha_close = heikinashi_.ha_close();
    [[maybe_unused]] auto op0 = heikinashi_[0];
}

TEST_CASE("Registry compile: Highest", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Highest highest_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    highest_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        highest_.next();
    }

    [[maybe_unused]] const auto& ref_line = highest_.line();
    [[maybe_unused]] auto op0 = highest_[0];
}

TEST_CASE("Registry compile: HurstExponent", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::HurstExponent hurstexponent_(data.close(), 40, 2, 0);

    // Process enough bars (preload sets index to 0)
    hurstexponent_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        hurstexponent_.next();
    }

    [[maybe_unused]] const auto& ref_line = hurstexponent_.line();
    [[maybe_unused]] auto op0 = hurstexponent_[0];
}

TEST_CASE("Registry compile: Ichimoku", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Ichimoku ichimoku_(data.high(), data.low(), data.close(), 9, 26, 52, 26, 26);

    // Process enough bars (preload sets index to 0)
    ichimoku_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        ichimoku_.next();
    }

    [[maybe_unused]] const auto& ref_tenkan_sen = ichimoku_.tenkan_sen();
    [[maybe_unused]] const auto& ref_kijun_sen = ichimoku_.kijun_sen();
    [[maybe_unused]] const auto& ref_senkou_span_a = ichimoku_.senkou_span_a();
    [[maybe_unused]] const auto& ref_senkou_span_b = ichimoku_.senkou_span_b();
    [[maybe_unused]] const auto& ref_chikou_span = ichimoku_.chikou_span();
    [[maybe_unused]] auto op0 = ichimoku_[0];
}

TEST_CASE("Registry compile: KAMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::KAMA kama_(data.close(), 14, 2, 30);

    // Process enough bars (preload sets index to 0)
    kama_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        kama_.next();
    }

    [[maybe_unused]] const auto& ref_line = kama_.line();
    [[maybe_unused]] auto op0 = kama_[0];
}

TEST_CASE("Registry compile: KAMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::KAMAEnvelope kamaenvelope_(data.close(), 30, 2, 30, 2.5);

    // Process enough bars (preload sets index to 0)
    kamaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        kamaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = kamaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = kamaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = kamaenvelope_.bottom();
    [[maybe_unused]] auto op0 = kamaenvelope_[0];
}

TEST_CASE("Registry compile: KAMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::KAMAOscillator kamaoscillator_(data.close(), 30, 2, 30);

    // Process enough bars (preload sets index to 0)
    kamaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        kamaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = kamaoscillator_.line();
    [[maybe_unused]] auto op0 = kamaoscillator_[0];
}

TEST_CASE("Registry compile: KST", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::KST kst_(data.close(), 10, 15, 20, 30, 10, 10, 10, 15, 9);

    // Process enough bars (preload sets index to 0)
    kst_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        kst_.next();
    }

    [[maybe_unused]] const auto& ref_kst = kst_.kst();
    [[maybe_unused]] const auto& ref_signal = kst_.signal();
    [[maybe_unused]] auto op0 = kst_[0];
}

TEST_CASE("Registry compile: KeltnerChannel", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::KeltnerChannel keltnerchannel_(data.high(), data.low(), data.close(), 20, 2.0);

    // Process enough bars (preload sets index to 0)
    keltnerchannel_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        keltnerchannel_.next();
    }

    [[maybe_unused]] const auto& ref_mid = keltnerchannel_.mid();
    [[maybe_unused]] const auto& ref_top = keltnerchannel_.top();
    [[maybe_unused]] const auto& ref_bottom = keltnerchannel_.bottom();
    [[maybe_unused]] auto op0 = keltnerchannel_[0];
}

TEST_CASE("Registry compile: KlingerOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::KlingerOscillator klingeroscillator_(data.high(), data.low(), data.close(), data.volume(), 34, 55, 13);

    // Process enough bars (preload sets index to 0)
    klingeroscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        klingeroscillator_.next();
    }

    [[maybe_unused]] const auto& ref_kvo = klingeroscillator_.kvo();
    [[maybe_unused]] const auto& ref_signal = klingeroscillator_.signal();
    [[maybe_unused]] auto op0 = klingeroscillator_[0];
}

TEST_CASE("Registry compile: LaguerreFilter", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::LaguerreFilter laguerrefilter_(data.close(), 0.5);

    // Process enough bars (preload sets index to 0)
    laguerrefilter_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        laguerrefilter_.next();
    }

    [[maybe_unused]] const auto& ref_line = laguerrefilter_.line();
    [[maybe_unused]] auto op0 = laguerrefilter_[0];
}

TEST_CASE("Registry compile: LaguerreRSI", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::LaguerreRSI laguerrersi_(data.close(), 0.5, 6);

    // Process enough bars (preload sets index to 0)
    laguerrersi_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        laguerrersi_.next();
    }

    [[maybe_unused]] const auto& ref_line = laguerrersi_.line();
    [[maybe_unused]] auto op0 = laguerrersi_[0];
}

TEST_CASE("Registry compile: Lowest", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Lowest lowest_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    lowest_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        lowest_.next();
    }

    [[maybe_unused]] const auto& ref_line = lowest_.line();
    [[maybe_unused]] auto op0 = lowest_[0];
}

TEST_CASE("Registry compile: MA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MA ma_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    ma_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        ma_.next();
    }

    [[maybe_unused]] const auto& ref_line = ma_.line();
    [[maybe_unused]] auto op0 = ma_[0];
}

TEST_CASE("Registry compile: MACD", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MACD macd_(data.close(), 12, 26, 9);

    // Process enough bars (preload sets index to 0)
    macd_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        macd_.next();
    }

    [[maybe_unused]] const auto& ref_macd = macd_.macd();
    [[maybe_unused]] const auto& ref_signal = macd_.signal();
    [[maybe_unused]] const auto& ref_histogram = macd_.histogram();
    [[maybe_unused]] auto op0 = macd_[0];
}

TEST_CASE("Registry compile: MACDHisto", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MACDHisto macdhisto_(data.close(), 12, 26, 9);

    // Process enough bars (preload sets index to 0)
    macdhisto_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        macdhisto_.next();
    }

    [[maybe_unused]] const auto& ref_macd = macdhisto_.macd();
    [[maybe_unused]] const auto& ref_signal = macdhisto_.signal();
    [[maybe_unused]] const auto& ref_histogram = macdhisto_.histogram();
    [[maybe_unused]] const auto& ref_histo = macdhisto_.histo();
    [[maybe_unused]] auto op0 = macdhisto_[0];
}

TEST_CASE("Registry compile: MeanDeviation", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MeanDeviation meandeviation_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    meandeviation_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        meandeviation_.next();
    }

    [[maybe_unused]] const auto& ref_line = meandeviation_.line();
    [[maybe_unused]] auto op0 = meandeviation_[0];
}

TEST_CASE("Registry compile: MinusDirectionalIndicator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MinusDirectionalIndicator minusdirectionalindicator_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    minusdirectionalindicator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        minusdirectionalindicator_.next();
    }

    [[maybe_unused]] const auto& ref_line = minusdirectionalindicator_.line();
    [[maybe_unused]] auto op0 = minusdirectionalindicator_[0];
}

TEST_CASE("Registry compile: Momentum", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Momentum momentum_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    momentum_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        momentum_.next();
    }

    [[maybe_unused]] const auto& ref_line = momentum_.line();
    [[maybe_unused]] auto op0 = momentum_[0];
}

TEST_CASE("Registry compile: MomentumOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MomentumOscillator momentumoscillator_(data.close(), 12);

    // Process enough bars (preload sets index to 0)
    momentumoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        momentumoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = momentumoscillator_.line();
    [[maybe_unused]] auto op0 = momentumoscillator_[0];
}

TEST_CASE("Registry compile: MoneyFlowIndex", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MoneyFlowIndex moneyflowindex_(data.high(), data.low(), data.close(), data.volume(), 14);

    // Process enough bars (preload sets index to 0)
    moneyflowindex_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        moneyflowindex_.next();
    }

    [[maybe_unused]] const auto& ref_line = moneyflowindex_.line();
    [[maybe_unused]] auto op0 = moneyflowindex_[0];
}

TEST_CASE("Registry compile: MovingAverageEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::MovingAverageEnvelope<stratforge::SMA> movingaverageenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    movingaverageenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        movingaverageenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = movingaverageenvelope_.src();
    [[maybe_unused]] const auto& ref_top = movingaverageenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = movingaverageenvelope_.bottom();
    [[maybe_unused]] auto op0 = movingaverageenvelope_[0];
}

TEST_CASE("Registry compile: NonZeroDifference", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::NonZeroDifference nonzerodifference_(sma_a_.line(), sma_b_.line());

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    nonzerodifference_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        nonzerodifference_.next();
    }

    [[maybe_unused]] const auto& ref_line = nonzerodifference_.line();
    [[maybe_unused]] auto op0 = nonzerodifference_[0];
}

TEST_CASE("Registry compile: OBV", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::OBV obv_(data.close(), data.volume());

    // Process enough bars (preload sets index to 0)
    obv_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        obv_.next();
    }

    [[maybe_unused]] const auto& ref_line = obv_.line();
    [[maybe_unused]] auto op0 = obv_[0];
}

TEST_CASE("Registry compile: OLS_Slope_InterceptN", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::OLS_Slope_InterceptN ols_slope_interceptn_(sma_a_.line(), sma_b_.line(), 10);

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    ols_slope_interceptn_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        ols_slope_interceptn_.next();
    }

    [[maybe_unused]] const auto& ref_slope = ols_slope_interceptn_.slope();
    [[maybe_unused]] const auto& ref_intercept = ols_slope_interceptn_.intercept();
    [[maybe_unused]] auto op0 = ols_slope_interceptn_[0];
}

TEST_CASE("Registry compile: Oscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::Oscillator oscillator_(sma_a_.line(), sma_b_.line());

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    oscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        oscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = oscillator_.line();
    [[maybe_unused]] auto op0 = oscillator_[0];
}

TEST_CASE("Registry compile: ParabolicSAR", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ParabolicSAR parabolicsar_(data.high(), data.low(), data.close(), 2, 0.02, 0.2);

    // Process enough bars (preload sets index to 0)
    parabolicsar_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        parabolicsar_.next();
    }

    [[maybe_unused]] const auto& ref_line = parabolicsar_.line();
    [[maybe_unused]] auto op0 = parabolicsar_[0];
}

TEST_CASE("Registry compile: PctRank", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PctRank pctrank_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    pctrank_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        pctrank_.next();
    }

    [[maybe_unused]] const auto& ref_line = pctrank_.line();
    [[maybe_unused]] auto op0 = pctrank_[0];
}

TEST_CASE("Registry compile: PercentChange", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PercentChange percentchange_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    percentchange_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        percentchange_.next();
    }

    [[maybe_unused]] const auto& ref_line = percentchange_.line();
    [[maybe_unused]] auto op0 = percentchange_[0];
}

TEST_CASE("Registry compile: PercentagePriceOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PercentagePriceOscillator percentagepriceoscillator_(data.close(), 12, 26, 9);

    // Process enough bars (preload sets index to 0)
    percentagepriceoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        percentagepriceoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_ppo = percentagepriceoscillator_.ppo();
    [[maybe_unused]] const auto& ref_signal = percentagepriceoscillator_.signal();
    [[maybe_unused]] const auto& ref_histo = percentagepriceoscillator_.histo();
    [[maybe_unused]] auto op0 = percentagepriceoscillator_[0];
}

TEST_CASE("Registry compile: PercentagePriceOscillatorShort", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PercentagePriceOscillatorShort percentagepriceoscillatorshort_(data.close(), 12, 26, 9);

    // Process enough bars (preload sets index to 0)
    percentagepriceoscillatorshort_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        percentagepriceoscillatorshort_.next();
    }

    [[maybe_unused]] const auto& ref_ppo = percentagepriceoscillatorshort_.ppo();
    [[maybe_unused]] const auto& ref_signal = percentagepriceoscillatorshort_.signal();
    [[maybe_unused]] const auto& ref_histo = percentagepriceoscillatorshort_.histo();
    [[maybe_unused]] auto op0 = percentagepriceoscillatorshort_[0];
}

TEST_CASE("Registry compile: PivotPoint", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PivotPoint pivotpoint_(data.open(), data.high(), data.low(), data.close(), false, false);

    // Process enough bars (preload sets index to 0)
    pivotpoint_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        pivotpoint_.next();
    }

    [[maybe_unused]] const auto& ref_p = pivotpoint_.p();
    [[maybe_unused]] const auto& ref_r1 = pivotpoint_.r1();
    [[maybe_unused]] const auto& ref_r2 = pivotpoint_.r2();
    [[maybe_unused]] const auto& ref_s1 = pivotpoint_.s1();
    [[maybe_unused]] const auto& ref_s2 = pivotpoint_.s2();
    [[maybe_unused]] auto op0 = pivotpoint_[0];
}

TEST_CASE("Registry compile: PlusDirectionalIndicator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PlusDirectionalIndicator plusdirectionalindicator_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    plusdirectionalindicator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        plusdirectionalindicator_.next();
    }

    [[maybe_unused]] const auto& ref_line = plusdirectionalindicator_.line();
    [[maybe_unused]] auto op0 = plusdirectionalindicator_[0];
}

TEST_CASE("Registry compile: PrettyGoodOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PrettyGoodOscillator prettygoodoscillator_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    prettygoodoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        prettygoodoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = prettygoodoscillator_.line();
    [[maybe_unused]] auto op0 = prettygoodoscillator_[0];
}

TEST_CASE("Registry compile: PriceOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PriceOscillator priceoscillator_(data.close(), 12, 26);

    // Process enough bars (preload sets index to 0)
    priceoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        priceoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_po = priceoscillator_.po();
    [[maybe_unused]] auto op0 = priceoscillator_[0];
}

TEST_CASE("Registry compile: PriceVolumeTrend", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::PriceVolumeTrend pricevolumetrend_(data.close(), data.volume());

    // Process enough bars (preload sets index to 0)
    pricevolumetrend_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        pricevolumetrend_.next();
    }

    [[maybe_unused]] const auto& ref_line = pricevolumetrend_.line();
    [[maybe_unused]] auto op0 = pricevolumetrend_[0];
}

TEST_CASE("Registry compile: ROC", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ROC roc_(data.close(), 12);

    // Process enough bars (preload sets index to 0)
    roc_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        roc_.next();
    }

    [[maybe_unused]] const auto& ref_line = roc_.line();
    [[maybe_unused]] auto op0 = roc_[0];
}

TEST_CASE("Registry compile: ROC100", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ROC100 roc100_(data.close(), 12);

    // Process enough bars (preload sets index to 0)
    roc100_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        roc100_.next();
    }

    [[maybe_unused]] const auto& ref_line = roc100_.line();
    [[maybe_unused]] auto op0 = roc100_[0];
}

TEST_CASE("Registry compile: RSI", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::RSI rsi_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    rsi_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        rsi_.next();
    }

    [[maybe_unused]] const auto& ref_line = rsi_.line();
    [[maybe_unused]] auto op0 = rsi_[0];
}

TEST_CASE("Registry compile: RSI_EMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::RSI_EMA rsi_ema_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    rsi_ema_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        rsi_ema_.next();
    }

    [[maybe_unused]] const auto& ref_line = rsi_ema_.line();
    [[maybe_unused]] auto op0 = rsi_ema_[0];
}

TEST_CASE("Registry compile: RSI_SMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::RSI_SMA rsi_sma_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    rsi_sma_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        rsi_sma_.next();
    }

    [[maybe_unused]] const auto& ref_line = rsi_sma_.line();
    [[maybe_unused]] auto op0 = rsi_sma_[0];
}

TEST_CASE("Registry compile: RSI_Safe", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::RSI_Safe rsi_safe_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    rsi_safe_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        rsi_safe_.next();
    }

    [[maybe_unused]] const auto& ref_line = rsi_safe_.line();
    [[maybe_unused]] auto op0 = rsi_safe_[0];
}

TEST_CASE("Registry compile: RSquared", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_a_(data.close(), 10);
    stratforge::SMA sma_b_(data.close(), 30);
    stratforge::RSquared rsquared_(sma_a_.line(), sma_b_.line(), 20);

    // Process enough bars (preload sets index to 0)
    sma_a_.next(); sma_b_.next();
    rsquared_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_a_.next(); sma_b_.next();
        rsquared_.next();
    }

    [[maybe_unused]] const auto& ref_line = rsquared_.line();
    [[maybe_unused]] auto op0 = rsquared_[0];
}

TEST_CASE("Registry compile: RelativeMomentumIndex", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::RelativeMomentumIndex relativemomentumindex_(data.close(), 20, 5);

    // Process enough bars (preload sets index to 0)
    relativemomentumindex_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        relativemomentumindex_.next();
    }

    [[maybe_unused]] const auto& ref_line = relativemomentumindex_.line();
    [[maybe_unused]] auto op0 = relativemomentumindex_[0];
}

TEST_CASE("Registry compile: SMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMA sma_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    sma_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sma_.next();
    }

    [[maybe_unused]] const auto& ref_line = sma_.line();
    [[maybe_unused]] auto op0 = sma_[0];
}

TEST_CASE("Registry compile: SMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMAEnvelope smaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    smaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        smaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = smaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = smaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = smaenvelope_.bottom();
    [[maybe_unused]] auto op0 = smaenvelope_[0];
}

TEST_CASE("Registry compile: SMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMAOscillator smaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    smaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        smaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = smaoscillator_.line();
    [[maybe_unused]] auto op0 = smaoscillator_[0];
}

TEST_CASE("Registry compile: SMMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMMA smma_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    smma_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        smma_.next();
    }

    [[maybe_unused]] const auto& ref_line = smma_.line();
    [[maybe_unused]] auto op0 = smma_[0];
}

TEST_CASE("Registry compile: SMMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMMAEnvelope smmaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    smmaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        smmaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = smmaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = smmaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = smmaenvelope_.bottom();
    [[maybe_unused]] auto op0 = smmaenvelope_[0];
}

TEST_CASE("Registry compile: SMMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SMMAOscillator smmaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    smmaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        smmaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = smmaoscillator_.line();
    [[maybe_unused]] auto op0 = smmaoscillator_[0];
}

TEST_CASE("Registry compile: StdDev", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::StdDev stddev_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    stddev_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        stddev_.next();
    }

    [[maybe_unused]] const auto& ref_line = stddev_.line();
    [[maybe_unused]] auto op0 = stddev_[0];
}

TEST_CASE("Registry compile: Stochastic", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Stochastic stochastic_(data.high(), data.low(), data.close(), 14, 3, 3);

    // Process enough bars (preload sets index to 0)
    stochastic_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        stochastic_.next();
    }

    [[maybe_unused]] const auto& ref_percK = stochastic_.percK();
    [[maybe_unused]] const auto& ref_percD = stochastic_.percD();
    [[maybe_unused]] auto op0 = stochastic_[0];
}

TEST_CASE("Registry compile: StochasticFast", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::StochasticFast stochasticfast_(data.high(), data.low(), data.close(), 14, 3);

    // Process enough bars (preload sets index to 0)
    stochasticfast_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        stochasticfast_.next();
    }

    [[maybe_unused]] const auto& ref_percK = stochasticfast_.percK();
    [[maybe_unused]] const auto& ref_percD = stochasticfast_.percD();
    [[maybe_unused]] auto op0 = stochasticfast_[0];
}

TEST_CASE("Registry compile: StochasticFull", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::StochasticFull stochasticfull_(data.high(), data.low(), data.close(), 14, 3, 3);

    // Process enough bars (preload sets index to 0)
    stochasticfull_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        stochasticfull_.next();
    }

    [[maybe_unused]] const auto& ref_percK = stochasticfull_.percK();
    [[maybe_unused]] const auto& ref_percD = stochasticfull_.percD();
    [[maybe_unused]] const auto& ref_percDSlow = stochasticfull_.percDSlow();
    [[maybe_unused]] auto op0 = stochasticfull_[0];
}

TEST_CASE("Registry compile: SumN", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SumN sumn_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    sumn_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        sumn_.next();
    }

    [[maybe_unused]] const auto& ref_line = sumn_.line();
    [[maybe_unused]] auto op0 = sumn_[0];
}

TEST_CASE("Registry compile: SuperTrend", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::SuperTrend supertrend_(data.high(), data.low(), data.close(), 10, 3.0);

    // Process enough bars (preload sets index to 0)
    supertrend_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        supertrend_.next();
    }

    [[maybe_unused]] const auto& ref_line = supertrend_.line();
    [[maybe_unused]] const auto& ref_upper_band = supertrend_.upper_band();
    [[maybe_unused]] const auto& ref_lower_band = supertrend_.lower_band();
    [[maybe_unused]] auto op0 = supertrend_[0];
}

TEST_CASE("Registry compile: T3MovingAverage", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::T3MovingAverage t3movingaverage_(data.close(), 5, 0.7);

    // Process enough bars (preload sets index to 0)
    t3movingaverage_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        t3movingaverage_.next();
    }

    [[maybe_unused]] const auto& ref_line = t3movingaverage_.line();
    [[maybe_unused]] auto op0 = t3movingaverage_[0];
}

TEST_CASE("Registry compile: TEMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TEMA tema_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    tema_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        tema_.next();
    }

    [[maybe_unused]] const auto& ref_line = tema_.line();
    [[maybe_unused]] auto op0 = tema_[0];
}

TEST_CASE("Registry compile: TEMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TEMAEnvelope temaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    temaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        temaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = temaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = temaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = temaenvelope_.bottom();
    [[maybe_unused]] auto op0 = temaenvelope_[0];
}

TEST_CASE("Registry compile: TEMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TEMAOscillator temaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    temaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        temaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = temaoscillator_.line();
    [[maybe_unused]] auto op0 = temaoscillator_[0];
}

TEST_CASE("Registry compile: TRIX", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TRIX trix_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    trix_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        trix_.next();
    }

    [[maybe_unused]] const auto& ref_line = trix_.line();
    [[maybe_unused]] auto op0 = trix_[0];
}

TEST_CASE("Registry compile: TSI", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TSI tsi_(data.close(), 25, 13, 1);

    // Process enough bars (preload sets index to 0)
    tsi_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        tsi_.next();
    }

    [[maybe_unused]] const auto& ref_line = tsi_.line();
    [[maybe_unused]] auto op0 = tsi_[0];
}

TEST_CASE("Registry compile: TrixSignal", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TrixSignal trixsignal_(data.close(), 15, 1, 9);

    // Process enough bars (preload sets index to 0)
    trixsignal_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        trixsignal_.next();
    }

    [[maybe_unused]] const auto& ref_line = trixsignal_.line();
    [[maybe_unused]] const auto& ref_signal = trixsignal_.signal();
    [[maybe_unused]] auto op0 = trixsignal_[0];
}

TEST_CASE("Registry compile: TrueHigh", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TrueHigh truehigh_(data.high(), data.close());

    // Process enough bars (preload sets index to 0)
    truehigh_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        truehigh_.next();
    }

    [[maybe_unused]] const auto& ref_line = truehigh_.line();
    [[maybe_unused]] auto op0 = truehigh_[0];
}

TEST_CASE("Registry compile: TrueLow", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TrueLow truelow_(data.low(), data.close());

    // Process enough bars (preload sets index to 0)
    truelow_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        truelow_.next();
    }

    [[maybe_unused]] const auto& ref_line = truelow_.line();
    [[maybe_unused]] auto op0 = truelow_[0];
}

TEST_CASE("Registry compile: TrueRange", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::TrueRange truerange_(data.high(), data.low(), data.close());

    // Process enough bars (preload sets index to 0)
    truerange_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        truerange_.next();
    }

    [[maybe_unused]] const auto& ref_line = truerange_.line();
    [[maybe_unused]] auto op0 = truerange_[0];
}

TEST_CASE("Registry compile: UlcerIndex", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::UlcerIndex ulcerindex_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    ulcerindex_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        ulcerindex_.next();
    }

    [[maybe_unused]] const auto& ref_line = ulcerindex_.line();
    [[maybe_unused]] auto op0 = ulcerindex_[0];
}

TEST_CASE("Registry compile: UltimateOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::UltimateOscillator ultimateoscillator_(data.high(), data.low(), data.close(), 7, 14, 28);

    // Process enough bars (preload sets index to 0)
    ultimateoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        ultimateoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = ultimateoscillator_.line();
    [[maybe_unused]] auto op0 = ultimateoscillator_[0];
}

TEST_CASE("Registry compile: UpDay", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::UpDay upday_(data.close(), 1);

    // Process enough bars (preload sets index to 0)
    upday_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        upday_.next();
    }

    [[maybe_unused]] const auto& ref_line = upday_.line();
    [[maybe_unused]] auto op0 = upday_[0];
}

TEST_CASE("Registry compile: UpDayBool", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::UpDayBool updaybool_(data.close(), 1);

    // Process enough bars (preload sets index to 0)
    updaybool_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        updaybool_.next();
    }

    [[maybe_unused]] const auto& ref_line = updaybool_.line();
    [[maybe_unused]] auto op0 = updaybool_[0];
}

TEST_CASE("Registry compile: UpMove", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::UpMove upmove_(data.high());

    // Process enough bars (preload sets index to 0)
    upmove_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        upmove_.next();
    }

    [[maybe_unused]] const auto& ref_line = upmove_.line();
    [[maybe_unused]] auto op0 = upmove_[0];
}

TEST_CASE("Registry compile: VIDYA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::VIDYA vidya_(data.close(), 14, 9);

    // Process enough bars (preload sets index to 0)
    vidya_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        vidya_.next();
    }

    [[maybe_unused]] const auto& ref_line = vidya_.line();
    [[maybe_unused]] auto op0 = vidya_[0];
}

TEST_CASE("Registry compile: VolumeWeightedAveragePrice", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::VolumeWeightedAveragePrice volumeweightedaverageprice_(data.high(), data.low(), data.close(), data.volume());

    // Process enough bars (preload sets index to 0)
    volumeweightedaverageprice_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        volumeweightedaverageprice_.next();
    }

    [[maybe_unused]] const auto& ref_line = volumeweightedaverageprice_.line();
    [[maybe_unused]] auto op0 = volumeweightedaverageprice_[0];
}

TEST_CASE("Registry compile: Vortex", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::Vortex vortex_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    vortex_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        vortex_.next();
    }

    [[maybe_unused]] const auto& ref_vi_plus = vortex_.vi_plus();
    [[maybe_unused]] const auto& ref_vi_minus = vortex_.vi_minus();
    [[maybe_unused]] auto op0 = vortex_[0];
}

TEST_CASE("Registry compile: WMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::WMA wma_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    wma_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        wma_.next();
    }

    [[maybe_unused]] const auto& ref_line = wma_.line();
    [[maybe_unused]] auto op0 = wma_[0];
}

TEST_CASE("Registry compile: WMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::WMAEnvelope wmaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    wmaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        wmaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = wmaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = wmaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = wmaenvelope_.bottom();
    [[maybe_unused]] auto op0 = wmaenvelope_[0];
}

TEST_CASE("Registry compile: WMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::WMAOscillator wmaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    wmaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        wmaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = wmaoscillator_.line();
    [[maybe_unused]] auto op0 = wmaoscillator_[0];
}

TEST_CASE("Registry compile: WeightedAverage", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::WeightedAverage weightedaverage_(data.close(), 14, 1.0);

    // Process enough bars (preload sets index to 0)
    weightedaverage_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        weightedaverage_.next();
    }

    [[maybe_unused]] const auto& ref_line = weightedaverage_.line();
    [[maybe_unused]] auto op0 = weightedaverage_[0];
}

TEST_CASE("Registry compile: WilliamsAD", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::WilliamsAD williamsad_(data.high(), data.low(), data.close());

    // Process enough bars (preload sets index to 0)
    williamsad_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        williamsad_.next();
    }

    [[maybe_unused]] const auto& ref_line = williamsad_.line();
    [[maybe_unused]] auto op0 = williamsad_[0];
}

TEST_CASE("Registry compile: WilliamsR", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::WilliamsR williamsr_(data.high(), data.low(), data.close(), 14);

    // Process enough bars (preload sets index to 0)
    williamsr_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        williamsr_.next();
    }

    [[maybe_unused]] const auto& ref_line = williamsr_.line();
    [[maybe_unused]] auto op0 = williamsr_[0];
}

TEST_CASE("Registry compile: ZLEMA", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ZLEMA zlema_(data.close(), 14);

    // Process enough bars (preload sets index to 0)
    zlema_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        zlema_.next();
    }

    [[maybe_unused]] const auto& ref_line = zlema_.line();
    [[maybe_unused]] auto op0 = zlema_[0];
}

TEST_CASE("Registry compile: ZLEMAEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ZLEMAEnvelope zlemaenvelope_(data.close(), 30, 2.5);

    // Process enough bars (preload sets index to 0)
    zlemaenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        zlemaenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = zlemaenvelope_.src();
    [[maybe_unused]] const auto& ref_top = zlemaenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = zlemaenvelope_.bottom();
    [[maybe_unused]] auto op0 = zlemaenvelope_[0];
}

TEST_CASE("Registry compile: ZLEMAOscillator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ZLEMAOscillator zlemaoscillator_(data.close(), 30);

    // Process enough bars (preload sets index to 0)
    zlemaoscillator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        zlemaoscillator_.next();
    }

    [[maybe_unused]] const auto& ref_line = zlemaoscillator_.line();
    [[maybe_unused]] auto op0 = zlemaoscillator_[0];
}

TEST_CASE("Registry compile: ZLIndicatorEnvelope", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ZLIndicatorEnvelope zlindicatorenvelope_(data.close(), 20, 50, 2.5);

    // Process enough bars (preload sets index to 0)
    zlindicatorenvelope_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        zlindicatorenvelope_.next();
    }

    [[maybe_unused]] const auto& ref_src = zlindicatorenvelope_.src();
    [[maybe_unused]] const auto& ref_top = zlindicatorenvelope_.top();
    [[maybe_unused]] const auto& ref_bottom = zlindicatorenvelope_.bottom();
    [[maybe_unused]] auto op0 = zlindicatorenvelope_[0];
}

TEST_CASE("Registry compile: ZeroLagIndicator", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ZeroLagIndicator zerolagindicator_(data.close(), 20, 50);

    // Process enough bars (preload sets index to 0)
    zerolagindicator_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        zerolagindicator_.next();
    }

    [[maybe_unused]] const auto& ref_line = zerolagindicator_.line();
    [[maybe_unused]] auto op0 = zerolagindicator_[0];
}

TEST_CASE("Registry compile: ZigZag", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::ZigZag zigzag_(data.close(), 0.05);

    // Process enough bars (preload sets index to 0)
    zigzag_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        zigzag_.next();
    }

    [[maybe_unused]] const auto& ref_line = zigzag_.line();
    [[maybe_unused]] auto op0 = zigzag_[0];
}

TEST_CASE("Registry compile: haDelta", "[registry][compile]") {
    stratforge::CsvData data(stratforge::CsvData::Params{
        .filename = std::string(SF_SOURCE_DIR) + "/tools/golden_extract/datas/2006-day-001.txt",
    });
    data.preload();

    stratforge::haDelta hadelta_(data.open(), data.high(), data.low(), data.close(), 3, true);

    // Process enough bars (preload sets index to 0)
    hadelta_.next();
    for (std::size_t i = 0; i < 59; ++i) {
        data.advance();
        hadelta_.next();
    }

    [[maybe_unused]] const auto& ref_ha_delta = hadelta_.ha_delta();
    [[maybe_unused]] const auto& ref_smoothed = hadelta_.smoothed();
    [[maybe_unused]] auto op0 = hadelta_[0];
}
