{
  "version": 1,
  "description": "StratForge performance regression baselines (TICKET_027 Phase B). Thresholds calibrated for shared CI runners with conservative headroom over local Release-build measurements. CI gate: fail if P50 regresses > 10% or P99 regresses > 20%.",
  "calibration_environment": {
    "build_type": "Release",
    "compiler": "gcc (linux x86_64)",
    "simd": "runtime-dispatched (AVX2 typical)",
    "notes": "Baselines reflect TICKET_027 spec (CLAUDE.md `Test Case Convention`). Values include CI VM headroom; local hardware may run 2-5x faster."
  },
  "baselines": [
    {
      "name": "SMA(30) per-bar",
      "tag": "[performance][regression][indicator]",
      "p50_max_ns": 200,
      "p99_max_ns": 500,
      "warmup_iterations": 1024,
      "measure_iterations": 10000
    },
    {
      "name": "BollingerBands(20,2) per-bar",
      "tag": "[performance][regression][indicator]",
      "p50_max_ns": 400,
      "p99_max_ns": 1000,
      "warmup_iterations": 1024,
      "measure_iterations": 10000
    },
    {
      "name": "Strategy SMA-crossover per-bar (end-to-end)",
      "tag": "[performance][regression][strategy]",
      "per_bar_max_ns": 5000,
      "bars": 5000,
      "notes": "End-to-end per-bar (Cerebro+Strategy+Indicators), single-shot amortized measurement (not a percentile distribution). Spec target 800ns is intra-next() only; 5000ns budget includes Cerebro framing + order routing overhead. Field is per_bar_max_ns to match tests/test_performance_gate.cpp:280 read pattern."
    },
    {
      "name": "Cerebro 10K-bar full backtest",
      "tag": "[performance][regression][engine]",
      "total_max_ns": 50000000,
      "per_bar_max_ns": 5000,
      "bars": 10000
    },
    {
      "name": "CSV load 10K rows",
      "tag": "[performance][regression][data]",
      "p50_max_ns": 20000000,
      "p99_max_ns": 40000000,
      "rows": 10000,
      "notes": "Full disk I/O + parse. Threshold includes CI disk variability."
    },
    {
      "name": "CSV per-row latency",
      "tag": "[performance][regression][data]",
      "p50_max_ns": 2000,
      "p99_max_ns": 5000,
      "notes": "Amortized per-row cost including date parsing and field conversion."
    },
    {
      "name": "Line::forward 10K elements",
      "tag": "[performance][regression][core]",
      "p50_max_ns": 100000,
      "p99_max_ns": 200000,
      "elements": 10000,
      "notes": "Pre-reserved append. Should be near vector::push_back throughput."
    },
    {
      "name": "Line::operator[] random access",
      "tag": "[performance][regression][core]",
      "p50_max_ns": 5000,
      "p99_max_ns": 10000,
      "accesses": 1000,
      "notes": "1000 random lookback accesses on 10K-element line."
    },
    {
      "name": "Order flood per-bar (market orders)",
      "tag": "[performance][regression][broker]",
      "p50_max_ns": 8000,
      "p99_max_ns": 15000,
      "notes": "Per-bar cost when submitting market order every bar (create + fill)."
    },
    {
      "name": "IncrementBatcher per-bar accumulation",
      "tag": "[performance][regression][observer]",
      "p50_max_ns": 200,
      "p99_max_ns": 500,
      "bars": 10000,
      "notes": "TICKET_789-B §7. Per-bar delta of Cerebro::run() with vs without an attached IncrementBatcher (noop callback, batch=10K so only one mid-flush). Local Release measurement: ~26-28 ns. p99_max_ns=500 carries CI VM headroom (CLAUDE.md convention)."
    },
    {
      "name": "IncrementBatcher flush dispatch",
      "tag": "[performance][regression][observer]",
      "p50_max_ns": 5000,
      "p99_max_ns": 10000,
      "bars": 10000,
      "notes": "TICKET_789-B §7. Per-flush upper-bound, computed as (with_batcher_ns − without_ns) / flush_count over a 10K-bar run with ~101 flushes. Upper bound because the divisor attributes all batcher overhead (including per-bar accumulation) to flushes alone — a strict over-estimate. Local Release measurement: ~1700 ns. Generous CI headroom to absorb the wall-clock noise floor on shared runners."
    }
  ]
}
